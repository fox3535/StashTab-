# Slice-02 inventory schema rehearsal — locked planning packet

**Slice:** `staging-readiness-v1 / slice-02-inventory-schema-rehearsal`  
**Status:** `PLAN CLOSED — LOCAL IMPLEMENTATION AWAITING APPROVAL`  
**Unlocked:** no  
**Pinned commit:** `d81e7c81aa03d72c1a236c481638808e9d05d759` (`main`)  
**Binding decisions:** D-027  
**Active contract:** `STASHTAB-INVENTORY-TRUTH-001` v1.2.0 (frozen; code on `main` via `c3647a4`; not deployed)

This packet is planning only. Do not implement, migrate, deploy, or enable
routes until a later named human approval of
`DIRECTIVE-SLICE-02-IMPLEMENTATION.md`.

---

## Binding owner decisions (D-027)

Recorded 2026-08-26. Interpretation is binding. Do not reopen.

1. A separate explicit live/base schema migrator must run before
   inventory-truth DDL.
2. Do **not** create all remaining live application tables.
3. Create only `inventory_item`, `purchase_record`, and `sale`, plus an
   extra table only when a current foreign-key dependency makes it
   unavoidable.
4. Every added dependency must be cited by model, column, constraint,
   and test. Convenience is not a dependency.
5. Add verified `shop_id → shops.id` foreign keys and the shop-scoped
   unique keys required by the frozen same-shop design.
6. First execution proof is disposable local PostgreSQL 16 only. Neon
   remains locked.
7. API receives SELECT only on rehearsal tables. No INSERT, UPDATE,
   DELETE, TRUNCATE, DDL, ownership, or migrator membership. Worker and
   readonly receive no access to those tables. Rollback preserves
   `shops`, `shop_members`, roles, and identity rows. Receive, POS,
   adjust, CSV quantity, Shopify, worker, notifications, Web Push,
   payments, Watch, and production stay unavailable.

**Rejected:** creating all remaining live tables. This slice proves the
inventory-truth parent chain. Unrelated tables would widen security,
rollback, privilege, testing, and product scope without required
evidence.

### Unavoidable extra live tables

**None.**

| Candidate | Model / column | SQLAlchemy `ForeignKey`? | Required now? |
|---|---|---|---|
| `staging_item` | intake writes it later; no FK from the three parents | no | no |
| `show_sessions` | `Sale.show_session_id` `String(36)` nullable | **no FK constraint** | no |
| `pending_trades` | no FK from the three parents | no | no |
| `shopify_credentials` / `sync_outbox` | no FK from the three parents | no | no |

Truth composite FKs target only `inventory_item`, `purchase_record`, and
`sale` (`app/inventory_truth/models_truth.py` `register_composite_fks`).

---

## Exact table inventory

### Already present (do not recreate)

| Table | Source |
|---|---|
| `shops` | identity migrator |
| `shop_members` | identity migrator |

Do not rerun identity `apply()` after extra tables exist. Its verifier
expects public tables to be exactly `{shops, shop_members}`.

### Live parents created this slice

`inventory_item`, `purchase_record`, `sale`.

### Truth tables created after live parents exist

`acquisition_lot`, `inventory_event`, `inventory_truth_cutover`,
`inventory_channel_observation`, `refund_record`, `return_record`,
`inventory_exception`, `inventory_adjustment`.

Notification tables are out of scope.

---

## Live column contract (from current models)

PostgreSQL mapping: `Integer` → `INTEGER`; autoincrement PK →
`INTEGER GENERATED BY DEFAULT AS IDENTITY`; `String(n)` → `VARCHAR(n)`;
`Float` → `DOUBLE PRECISION`; `Boolean` → `BOOLEAN`;
`DateTime(timezone=True)` → `TIMESTAMP WITH TIME ZONE`.

Python `default=` on the ORM is **not** a database DEFAULT. Identity
style is preserved: no database defaults; inserts in tests supply values.

`shop_id` on all three: `VARCHAR(36) NOT NULL`.

### `inventory_item` (`InventoryItem`)

| Column | PG type | Null | Notes |
|---|---|---|---|
| `id` | `INTEGER GENERATED BY DEFAULT AS IDENTITY` | no | PK |
| `shop_id` | `VARCHAR(36)` | no | FK → `shops.id` |
| `sku` | `VARCHAR(50)` | no | |
| `name` | `VARCHAR(100)` | no | |
| `set_name` | `VARCHAR(100)` | yes | |
| `sequence_number` | `VARCHAR(50)` | yes | |
| `cost` | `DOUBLE PRECISION` | no | snapshot float; do not change type |
| `price` | `DOUBLE PRECISION` | no | |
| `old_price` | `DOUBLE PRECISION` | yes | |
| `card_type` | `VARCHAR(50)` | yes | |
| `variant` | `VARCHAR(50)` | yes | |
| `condition` | `VARCHAR(50)` | yes | |
| `stock` | `INTEGER` | no | |
| `last_sync` | `TIMESTAMPTZ` | yes | |
| `date_added` | `TIMESTAMPTZ` | no | |
| `needs_update` | `BOOLEAN` | no | |
| `needs_review` | `BOOLEAN` | no | |
| `image_url` | `VARCHAR(512)` | yes | |
| `image_locked` | `BOOLEAN` | no | |
| `sync_status` | `VARCHAR(50)` | no | |
| `custom_image_url` | `VARCHAR(512)` | yes | |
| `shop_listing_price` | `DOUBLE PRECISION` | yes | |
| `sticker_price` | `DOUBLE PRECISION` | yes | |
| `paused_stock` | `INTEGER` | no | |
| `game` | `VARCHAR(50)` | no | |
| `created_at` | `TIMESTAMPTZ` | no | |
| `updated_at` | `TIMESTAMPTZ` | no | |

### `purchase_record` (`PurchaseRecord`)

| Column | PG type | Null |
|---|---|---|
| `id` | `INTEGER GENERATED BY DEFAULT AS IDENTITY` | no |
| `shop_id` | `VARCHAR(36)` | no |
| `sku` | `VARCHAR(50)` | no |
| `quantity` | `INTEGER` | no |
| `cost_per_unit` | `DOUBLE PRECISION` | no |
| `timestamp` | `TIMESTAMPTZ` | no |
| `created_at` | `TIMESTAMPTZ` | no |
| `updated_at` | `TIMESTAMPTZ` | no |

### `sale` (`Sale`)

| Column | PG type | Null | Notes |
|---|---|---|---|
| `id` | `INTEGER GENERATED BY DEFAULT AS IDENTITY` | no | PK |
| `shop_id` | `VARCHAR(36)` | no | FK → `shops.id` |
| `item_name` | `VARCHAR(100)` | yes | |
| `sku` | `VARCHAR(50)` | yes | |
| `sold_price` | `DOUBLE PRECISION` | yes | |
| `profit` | `DOUBLE PRECISION` | yes | |
| `timestamp` | `TIMESTAMPTZ` | no | |
| `transaction_type` | `VARCHAR(20)` | yes | |
| `trade_in_value` | `DOUBLE PRECISION` | no | |
| `processing_fees` | `DOUBLE PRECISION` | no | |
| `trade_credit_deduction` | `DOUBLE PRECISION` | no | |
| `net_revenue` | `DOUBLE PRECISION` | no | |
| `game` | `VARCHAR(50)` | no | |
| `show_session_id` | `VARCHAR(36)` | yes | **plain column; no FK** |
| `is_reconciled` | `BOOLEAN` | no | |
| `created_at` | `TIMESTAMPTZ` | no | |
| `updated_at` | `TIMESTAMPTZ` | no | |

Truth table types remain those already frozen in
`app/inventory_truth/models_truth.py`. This slice does not amend them.
Lot money stays `NUMERIC(12,2)`. Snapshot floats stay floats.

---

## Index, unique, check, and foreign-key order

### Live migrator (new), one transaction

1. `CREATE TABLE inventory_item` (columns + PK only).
2. `CREATE TABLE purchase_record`.
3. `CREATE TABLE sale`.
4. Non-unique indexes from current models:
   - `ix_inventory_item_shop_id` (`shop_id`)
   - `ix_inventory_item_sku` (`sku`)
   - `ix_purchase_record_shop_id` (`shop_id`)
   - `ix_purchase_record_sku` (`sku`)
   - `ix_sale_shop_id` (`shop_id`)
   - `ix_sale_sku` (`sku`)
5. Model unique: `uq_inventory_shop_sku` on `inventory_item (shop_id, sku)`.
6. Frozen same-shop uniques (required before truth composite FKs):
   - `uq_inventory_item_shop_id` on `(shop_id, id)`
   - `uq_purchase_record_shop_id` on `(shop_id, id)`
   - `uq_sale_shop_id` on `(shop_id, id)`
7. Shop FKs, `ON DELETE RESTRICT`:
   - `fk_inventory_item_shop_id`
   - `fk_purchase_record_shop_id`
   - `fk_sale_shop_id`
8. `OWNER TO stashtab_migrator`.
9. Privilege pass (below).
10. Injected-failure hook after tables, after uniques, and after FKs.

No CHECK constraints exist on these three models except via mixin none.

### Truth migrator (existing), after live parents exist, one transaction

Keep committed order: unique `(shop_id, id)` if missing → truth tables
with `register_composite_fks` → append-only triggers on
`refund_record`, `return_record`, `inventory_adjustment` → TRUNCATE deny
→ `inventory_event.lot_id` nullable.

Composite FKs (already in code, `ON DELETE RESTRICT`):

| Child | Columns | Parent |
|---|---|---|
| `acquisition_lot` | `(shop_id, inventory_item_id)` | `inventory_item` |
| `acquisition_lot` | `(shop_id, purchase_record_id)` | `purchase_record` |
| `inventory_event` | `(shop_id, lot_id)` | `acquisition_lot` |
| `inventory_event` | `(shop_id, inventory_item_id)` | `inventory_item` |
| `inventory_event` | `(shop_id, sale_id)` | `sale` |
| `inventory_event` | `(shop_id, reverses_event_id)` | `inventory_event` |
| `inventory_channel_observation` | `(shop_id, sale_id)` | `sale` |
| `refund_record` | `(shop_id, outbound_event_id)` | `inventory_event` |
| `return_record` | `(shop_id, refund_record_id)` | `refund_record` |
| `return_record` | `(shop_id, outbound_event_id)` | `inventory_event` |
| `inventory_adjustment` | `(shop_id, inventory_event_id)` | `inventory_event` |
| `inventory_adjustment` | `(shop_id, inventory_item_id)` | `inventory_item` |
| `inventory_adjustment` | `(shop_id, reverses_event_id)` | `inventory_event` |

`inventory_exception` has no composite parent FK; tests still require
`shop_id` on every insert.

---

## Ownership, default privileges, grants

Roles already exist. Do not `CREATE ROLE` in this slice.

| Object | Owner | `stashtab_api` | `stashtab_worker` | `stashtab_readonly` | `PUBLIC` |
|---|---|---|---|---|---|
| `shops`, `shop_members` | `stashtab_migrator` | SELECT, INSERT (slice-01) | none | none | none |
| `inventory_item`, `purchase_record`, `sale` | `stashtab_migrator` | **SELECT only** | **none** | **none** | none |
| eight truth tables | `stashtab_migrator` | **SELECT only** | **none** | **none** | none |
| sequences on new integer PKs | `stashtab_migrator` | **none** | none | none | none |
| schema `public` CREATE | n/a | revoked | revoked | revoked | revoked |

After each new table:

- `REVOKE ALL ON table FROM PUBLIC, stashtab_api, stashtab_worker, stashtab_readonly`
- `GRANT SELECT ON table TO stashtab_api`
- `ALTER DEFAULT PRIVILEGES IN SCHEMA public` for migrator: revoke ALL
  on tables from PUBLIC / api / worker / readonly, then grant SELECT to
  api only for this slice’s tables
- No `GRANT … TO stashtab_api` of INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/
  TRIGGER
- API must not be a member of `stashtab_migrator`

---

## Apply and rollback order

**Apply (operator, local PG 16):**

1. Confirm identity tables and roles exist. Do not recreate them.
2. Live migrator apply (steps above).
3. Truth migrator apply (existing `app.inventory_truth.migrator.apply`).
4. Verify table list, uniques, FKs, grants, and that API cannot DDL.

**Rollback (operator, local PG 16):**

1. As `stashtab_migrator`, drop truth tables children-first:
   `inventory_adjustment`, `return_record`, `refund_record`,
   `inventory_exception`, `inventory_channel_observation`,
   `inventory_event`, `acquisition_lot`, `inventory_truth_cutover`.
2. Drop live parents: `sale`, `purchase_record`, `inventory_item`.
3. Do **not** drop `shops`, `shop_members`, or roles.
4. Verify Smoke Shop A/B identity rows remain.

Do not call identity `rollback()`.

---

## Disposable PostgreSQL 16 fixture

Target: empty-of-inventory database matching Neon start state.

1. PostgreSQL 16 container. Create roles with
   `sql/provision-staging-roles.sql` (local names only).
2. Apply identity migrator. Insert two shops and two distinct owners
   (Smoke Shop A / Smoke Shop B), same shape as slice-01 synthetic shops.
3. Run live migrator, then truth migrator.
4. Schema proofs use **zero** inventory rows first.
5. FK negative tests insert one `inventory_item` and one `sale` per shop
   (`SKU-A-1`, `SKU-B-1`) with explicit NOT NULL values. No production
   catalog, no Shopify ids, no customer PII.
6. Destroy the container after the run.

Neon is not a target. No Railway, Clerk, or production credentials.

---

## Partner-Python map (these tables only)

| Partner | StashTab | Preserve | Deviation | Proof |
|---|---|---|---|---|
| `database.py` inventory / purchase / sale models | `InventoryItem`, `PurchaseRecord`, `Sale` | SKU snapshot, purchase row, sale line | `shop_id`; Clerk; no staging `create_all` | live-schema tests |
| `logic.py` intake commit that writes vault rows | `app/logic/intake.py` | Persistent SKU onto `inventory_item` | Dual-write and `staging_item` table not in this slice | FEATURE_NOT_READY remains |
| `logic.py` trade that writes `purchase_record` | `app/logic/trades.py` | Purchase row as acquisition evidence | Dual-write gated | 503 on staging routes |
| `logic.py` `complete_sale` | `app/logic/sales.py` | One sale line, snapshot decrement | Outbound truth gated | POS stays unavailable |
| `database.py` wipe helpers | none | Do not port | Destructive wipe forbidden | no route |

`staging_item`, Shopify, Collectr recon, and worker functions are
**not** in this slice.

---

## Local acceptance criteria

Must pass on disposable PG 16 before any later hosted unlock:

1. Starting catalog is identity-only; live/truth absent.
2. Live apply is atomic; `fail_after` leaves zero live tables.
3. Second live apply is a no-op.
4. Truth apply fails if live parents are missing.
5. Truth apply succeeds after live parents; second run is a no-op.
6. Truth `fail_after` rolls back truth objects; live parents remain;
   identity rows remain.
7. Exact table list = identity 2 + live 3 + truth 8.
8. Unique `(shop_id, id)` exists on the three live parents.
9. `shop_id` FKs to `shops` reject unknown shops.
10. Cross-shop lot/item/sale composite FK inserts fail.
11. Connected as `stashtab_api`: `SELECT` on rehearsal tables succeeds;
    INSERT/UPDATE/DELETE/TRUNCATE/CREATE/ALTER/DROP fail; `SET ROLE
    stashtab_migrator` fails.
12. `stashtab_worker` and `stashtab_readonly` have no table privileges on
    rehearsal tables.
13. API boot `APP_ENV=staging` does not create/alter tables.
14. Receive/POS/adjust/CSV quantity still return `503 FEATURE_NOT_READY`
    (no completed cutover; dual-write off).
15. Worker process is not started.

---

## Terminal outcome and attempt limit

| State | Meaning |
|---|---|
| `PLAN_CLOSED` | this packet |
| `LOCAL_IMPLEMENTATION_AWAITING_APPROVAL` | wait for named approval of the directive |
| `UNLOCKED_LOCAL_ONLY` | later named unlock for local PG 16 only |
| `REJECTED_STOP` | freeze rewrite requested, Neon touch requested, or extra tables requested without FK proof |

Planning budget is consumed: one wording lock, one consistency
verification. A further planning rewrite is not success.

This slice does **not** close the F0 exit gate. It only prepares
schema-chain evidence.

---

## Smallest proposed implementation directive

See `DIRECTIVE-SLICE-02-IMPLEMENTATION.md`. Not approved. Local PG 16
only. Two migrator steps: live parents, then existing truth migrator.
No Neon, no route enablement, no extra live tables.
