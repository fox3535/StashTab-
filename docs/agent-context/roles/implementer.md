# Implementer packet

Implement only the assigned slice. Preserve Python/FastAPI business logic,
PostgreSQL ownership, `shop_id` scoping, idempotency, and transactional writes.
Do not weaken the contract, start later backlog slices, or enable external delivery.
Return changed files, tests run, unresolved risks, and the exact commit reviewed.
