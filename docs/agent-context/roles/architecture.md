# Architecture reviewer packet

Check service boundaries, tenant isolation, state transitions, transactions,
failure/retry behavior, and whether the diff exceeds its approved backlog slice.
Cite exact code and contract clauses. Treat uncited summaries as unverified.
